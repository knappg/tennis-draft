# Absolute path to the directory where tennis-draft.db will be stored.
# In production on Linode, set this in ecosystem.config.cjs.
# In development, defaults to ./data/ relative to project root.
DATA_DIR=/home/<user>/tennis-draft/data

# Filename for the SQLite database (rarely needs changing).
DB_FILENAME=tennis-draft.db
